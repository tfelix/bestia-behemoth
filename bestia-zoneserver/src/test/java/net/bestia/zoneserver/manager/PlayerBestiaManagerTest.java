package net.bestia.zoneserver.manager;

import net.bestia.model.domain.PlayerBestia;
import net.bestia.zoneserver.Zoneserver;

import org.junit.Test;

// TODO DAS HIER FERTIG MACHEN.
public class PlayerBestiaManagerTest {
	

	@Test
	public void check_init_of_transient_values_test() {
		
	}
	
	/*
	public PlayerBestiaManager getManager() {
		/*final PlayerBestia bestia = new PlayerBestia();
		
		bestia.setAttack1(attack1);
		bestia.setAttack2(attack2);
		
		bestia.setCurrentHp(curHp);
		bestia.setCurrentMana(curMana);
		bestia.setCurrentPosition(currentPosition);
		
		bestia.setItem1(item1);
		bestia.setItem2(item2);
		
		bestia.setLevel(level);
		bestia.setName(name);
		bestia.setSavePosition(savePosition);
		
		bestia.set
		
		final PlayerBestiaManager manager = new PlayerBestiaManager(bestia, world, entity, server, locator)
	}*/
}
