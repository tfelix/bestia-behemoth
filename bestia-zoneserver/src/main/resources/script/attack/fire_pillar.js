function onTick() {
	Bestia.info('Hello from script.');
	// Get all Bestias in Range of fire_pillar

	// Make fire Damage
}

Bestia.createEntity()
    .setLivetime(entityId, 12000)
    .setInterval(entityId, 1000, 'onTick');
