package net.bestia.zoneserver.zone.wecs;

/**
 * Responsible for reading the temperature and sky humidity layer in order to
 * check if there will be rain. The rain will only for if there are enough
 * patches adjacent to each other in order to cover a area big enough for rain
 * to plausible form.
 * 
 * @author Thomas Felix <thomas.felix@tfelix.de>
 *
 */
public class RainLayerExchanger implements Exchanger {

	public void calculateExchange(EnvironmentManager envManager) {
		
	}
}
