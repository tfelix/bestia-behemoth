package net.bestia.zone

import org.springframework.stereotype.Component

/**
 * Configuration which can be changed on demand during runtime of the server and does
 * not require a server reload.
 */
@Component
class RuntimeConfig()