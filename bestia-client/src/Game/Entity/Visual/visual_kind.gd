class_name VisualKind

## Mirrors VisualComponentSMSG.VisualKind on the C# side. An exported C# enum reaches GDScript as a
## plain int, so these names exist to keep the dispatch in entity.gd readable.

const BESTIA := 0
const ITEM := 1
const EFFECT := 2
